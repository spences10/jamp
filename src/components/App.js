import React, { Component } from 'react'
import Markdown from './Markdown'

class App extends Component {
  render() {
    return (
      <React.Fragment>
        <Markdown />
      </React.Fragment>
    )
  }
}

export default App
